function findWay(arr)
{
    var ar2D = new Array();
    for (var col = 0 ; col < arr.length; col++)
    {
        for (var row = 0; row < arr[col].length; row++)
        {
            if (arr[row][col]==0)
            {
                
            }
        }
    }
}